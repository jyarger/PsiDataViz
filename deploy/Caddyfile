# Caddy reverse proxy with automatic HTTPS.
# MOLVIZ_DOMAIN is supplied by docker-compose (defaults to localhost for local testing).
{$MOLVIZ_DOMAIN:localhost} {
	encode gzip zstd
	reverse_proxy app:8050
}
